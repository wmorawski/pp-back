type Query {
  parties(
    where: PartyWhereInput
    orderBy: PartyOrderByInput
    skip: Int
    after: String
    before: String
    first: Int
    last: Int
  ): [Party]!
}

type Mutation {
  createParty(data: PartyCreateInput!): Party!
}
